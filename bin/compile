#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>
export OUT_PATH=/app/vendor/libtiff

echo "-----> Installing libtiff"

# change to the the BUILD_DIR ($1)
cd $1

# download the mruby binary (-O) silently (-s)
curl https://github.com/AkuRaPopo/heroku-buildpack-libtiff/raw/master/libtiff403.tar.gz -s -O

# make a directory to untar (like unzip) the binary
mkdir -p vendor/libtiff

# untar the binary to the directory we want
tar -C vendor/libtiff -xvf libtiff403.tar.gz

# delete tmp
rm -rf libtiff403.tar.gz
